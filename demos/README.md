# Demos

`demos/` 只放用于运行、编译或复现具体行为的最小示例。纯文字解释应放入 `notes/`。

## Java

- [Linkage error 示例](./java/linkage-error/)

通常不提交生成的 `.class`、`target/`、`build/` 或 `out/`。`linkage-error` 目录中的两个 `.class` 是复现类加载行为所需的固定测试材料，因此在 `.gitignore` 中被明确保留。

返回 [仓库首页](../README.md)。
